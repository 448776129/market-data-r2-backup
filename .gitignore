# K 线数据已迁移至 Cloudflare R2，不再提交到 git
# 仅保留 data/universe/ 股票清单（供 GitHub Actions 读取）
data/*/
!data/universe/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# 环境变量
.env
.env.local

# 敏感文件（含真实凭据，禁止提交到公开仓库）
SECRETS.md

# 项目记忆：含账号 ID / 服务器 IP / 内网结构，仅限本地，禁止提交到公开仓库
# 真实凭据统一存放于 .env（同样不提交）
PROJECT_MEMORY.md

# CodeBuddy 项目记忆（本地开发记录，不公开）
.codebuddy/

# Node（api/ 的依赖，仅提交源码）
api/node_modules/
api/dist/

# 本地临时文件
scripts/_test_*.py
